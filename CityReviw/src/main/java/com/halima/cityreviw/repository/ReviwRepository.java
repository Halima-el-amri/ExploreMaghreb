package com.halima.cityreviw.repository;

import com.halima.cityreviw.model.Reviw;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ReviwRepository  extends JpaRepository<Reviw, Long> {
}
